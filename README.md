# 🚀 Запуск проекта

## Быстрый старт

Для запуска всего проекта выполните команду:

```bash
npm run dev
```

Эта команда запустит:
- **Backend API сервер** на порту 5000: http://localhost:5000
- **Frontend сервер** на порту 5001: http://localhost:5001

## Структура проекта

```
├── backend/
│   └── src/
│       ├── config/
│       │   └── database.js      # Конфигурация PostgreSQL
│       ├── middleware/
│       │   └── auth.js          # JWT аутентификация
│       ├── models/
│       │   └── User.js          # Модель пользователя
│       ├── routes/
│       │   ├── auth.js          # Роуты регистрации/авторизации
│       │   └── user.js          # Роуты личного кабинета
│       └── server.js            # Основной файл сервера
├── frontend/
│   ├── public/
│   │   ├── css/
│   │   │   └── styles.css       # Стили приложения
│   │   ├── js/
│   │   │   └── common.js        # Общие JS функции
│   │   ├── index.html           # Главная страница
│   │   ├── login.html           # Страница входа
│   │   ├── dashboard.html       # Личный кабинет
│   │   └── register.html        # Страница регистрации
│   └── server.js                # Фронтенд сервер
└── .env                         # Переменные окружения
```

## Доступные команды

- `npm run dev` - Запуск обоих серверов (backend + frontend)
- `npm run dev:backend` - Запуск только backend сервера
- `npm run dev:frontend` - Запуск только frontend сервера
- `npm start` - Production запуск backend
- `npm run setup-db` - Настройка базы данных

## API эндпоинты

### Аутентификация
- `POST /api/auth/register-step1` - Первый этап регистрации (email, пароль)
- `POST /api/auth/register-step2` - Второй этап регистрации (ФИО, дата рождения)
- `POST /api/auth/login` - Вход в систему

### Пользователь
- `GET /api/user/profile` - Получение данных профиля
- `GET /api/user/birthday-check` - Проверка дня рождения

### Системные
- `GET /api/health` - Проверка работы API

## База данных

Проект использует PostgreSQL. Настройки подключения в файле `.env`:


## Особенности

- Двухэтапная регистрация
- JWT аутентификация
- Автоматические поздравления в день рождения
- CORS настроен для работы с фронтендом на порту 5001
- Сессии для временного хранения данных регистрации

## Если что-то не работает

1. **Порты заняты**: Освободите порты 5000 и 5001
   ```bash
   netstat -ano | findstr :5000
   netstat -ano | findstr :5001
   taskkill /F /PID <PID_процесса>
   ```

2. **База данных не доступна**: Убедитесь, что PostgreSQL запущен

3. **Проверьте работу API**: 
   ```bash
   curl http://localhost:5000/api/health
   ```

4. **Перезапустите проект**:
   ```bash
   npm run dev
   ```
